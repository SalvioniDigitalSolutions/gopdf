name: test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        include:
          # The version go.mod declares, to prove the floor still builds.
          # Only on Linux: a binary from this toolchain carries no
          # LC_UUID, and current macOS refuses to launch one, so every
          # test binary aborts before it runs a test.
          - os: ubuntu-latest
            go: '1.22'
          - os: ubuntu-latest
            go: stable
          - os: macos-latest
            go: stable
    runs-on: ${{ matrix.os }}
    name: ${{ matrix.os }} / go ${{ matrix.go }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: ${{ matrix.go }}
          check-latest: true

      # Several tests measure this package against an independent
      # implementation, and several others need a real font file to
      # embed. Both are skipped when absent rather than failing, so
      # installing them is the difference between the suite running and
      # the suite quietly stepping over its most interesting parts.
      - name: Install Poppler and a font
        if: matrix.os == 'ubuntu-latest'
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends poppler-utils fonts-dejavu-core

      - name: Install Poppler
        if: matrix.os == 'macos-latest'
        run: brew install poppler

      - name: gofmt
        run: |
          unformatted=$(gofmt -l .)
          if [ -n "$unformatted" ]; then
            echo "not gofmt'd:"; echo "$unformatted"; exit 1
          fi

      - name: go vet
        run: go vet ./...

      - name: build
        run: go build ./...

      - name: test
        run: go test ./... -timeout 20m

      - name: test -race
        run: go test -race ./... -timeout 30m
