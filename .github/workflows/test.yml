name: test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        include:
          # The version go.mod declares, to prove the floor still builds.
          # Only on Linux: a binary from this toolchain carries no
          # LC_UUID, and current macOS refuses to launch one, so every
          # test binary aborts before it runs a test.
          - os: ubuntu-latest
            go: '1.22'
          - os: ubuntu-latest
            go: stable
          - os: macos-latest
            go: stable
    runs-on: ${{ matrix.os }}
    name: ${{ matrix.os }} / go ${{ matrix.go }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: ${{ matrix.go }}
          check-latest: true

      # Several tests measure this package against an independent
      # implementation, and several others need a real font file to
      # embed. Both are skipped when absent rather than failing, so
      # installing them is the difference between the suite running and
      # the suite quietly stepping over its most interesting parts.
      - name: Install Poppler and a font
        if: matrix.os == 'ubuntu-latest'
        # A package mirror that stops answering hangs the job until the
        # whole workflow times out, which has happened. Bound it, and try
        # again rather than losing the run to one bad mirror.
        timeout-minutes: 6
        run: |
          for attempt in 1 2 3; do
            if sudo apt-get update && sudo apt-get install -y \
                --no-install-recommends poppler-utils fonts-dejavu-core; then
              exit 0
            fi
            echo "apt attempt $attempt failed; retrying"
            sleep 10
          done
          exit 1

      - name: Install Poppler
        if: matrix.os == 'macos-latest'
        timeout-minutes: 10
        run: brew install poppler

      - name: gofmt
        run: |
          unformatted=$(gofmt -l .)
          if [ -n "$unformatted" ]; then
            echo "not gofmt'd:"; echo "$unformatted"; exit 1
          fi

      - name: go vet
        run: go vet ./...

      - name: build
        run: go build ./...

      - name: test
        timeout-minutes: 25
        run: go test ./... -timeout 20m

      - name: test -race
        timeout-minutes: 35
        run: go test -race ./... -timeout 30m
